{
  "name": "@pg-rocket/bench",
  "version": "0.0.0",
  "private": true,
  "description": "pg-rocket vs pg vs postgres.js head-to-head benchmarks (local-only).",
  "type": "module",
  "scripts": {
    "seed": "node --experimental-strip-types src/seed.ts",
    "bench": "node --experimental-strip-types src/run.ts",
    "typecheck": "tsgo --noEmit"
  },
  "dependencies": {
    "pg": "^8.13.0",
    "pg-rocket": "workspace:*",
    "postgres": "^3.4.4"
  },
  "devDependencies": {
    "@types/pg": "^8.11.0"
  }
}
